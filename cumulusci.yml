minimum_cumulusci_version: '3.92.0'
project:
    name: CumulusCI
    package:
        name: CumulusCITrail
        namespace: NSpaceSales
        api_version: '61.0'
    git:
    source_format: sfdx

tasks:
    robot:
        options:
            suites: robot/CumulusCI/tests
            options:
                outputdir: robot/CumulusCI/results

    robot_testdoc:
        options:
            path: robot/CumulusCI/tests
            output: robot/CumulusCI/doc/CumulusCI_tests.html

